/** Child device — loud emergency alert while incoming call is ringing */
export const CHILD_RINGTONE = require('../../assets/ringtone.mp3');

/** Parent device — local ringback while waiting for the child to answer */
export const PARENT_RINGBACK = require('../../assets/ringback.mp3');
